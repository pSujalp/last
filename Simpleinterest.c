#include<stdio.h>
#include<conio.h>

int  main()
{
    int a,b,c,d;
    printf("Print Principal \n Rate of interest per annum \n Time");
   scanf("%d %d %d",&a,&b,&c);
    d=a*b*c;
    printf("simple interest %d",d);
    return 0;
}